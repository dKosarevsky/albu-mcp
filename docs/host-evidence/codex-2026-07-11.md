# Codex Host Acceptance Receipt - 2026-07-11

## Scope

This receipt records a reviewer-observed interactive Codex TUI session using the installed personal
`albumentationsx-mcp` plugin. It proves host discovery and the first-ten-minutes workflow over a deliberately simple
local fixture. It does not represent external beta feedback, a private production dataset, or adoption evidence.

## Environment

- Host: Codex TUI
- Host session: `019f51d1-45dc-7581-adfc-fc28ae81706e`
- Plugin/package version: `1.15.0`
- AlbumentationsX version reported by the MCP server: `2.3.1`
- Transport: local stdio through the installed Codex plugin
- Dataset: one locally generated 160 x 120 image containing a grid, rectangle, circle, and `sample object` label

An earlier interactive discovery session, `019f51cd-c985-7f01-89b7-f3a962118ad6`, confirmed that Codex loaded the
plugin and exposed the MCP tools before the complete replay began.

## Observed Workflow

1. `run_host_smoke_check` returned `status=ok`, `preview_ready=true`, six passing diagnostics, and no warnings.
2. The first onboarding request used the exact image path. Version `1.15.0` rejected it with
   `dataset_path_not_directory`; retrying its parent directory produced a preview-ready plan.
3. The baseline preview completed as run `be02b117aefa467d98365844805c8aba`.
4. The host reviewed the baseline and requested visibly stronger brightness/contrast variation. The `1.15.0`
   feedback catalog had no matching “too weak” tag, so the host inspected the transform schema and adjusted the
   exposure transform explicitly.
5. The candidate preview completed as run `359c5689201046ed83a7a35dde2c09e3` with this pipeline:
   - `HorizontalFlip`, `p=0.2`
   - `RandomBrightnessContrast`, brightness and contrast limits `[-0.3, 0.3]`, `p=1.0`
   - `GaussNoise`, standard-deviation range `[0.015, 0.075]`, `p=0.075`
   - `MotionBlur`, blur range `[3, 5]`, `p=0.05`
6. Candidate validation returned no errors or warnings. Quality analysis returned score `85`, risk `medium`, and a
   `candidate_too_bright` warning; the reviewer confirmed that the object geometry and label remained readable.
7. The accepted decision was recorded as `b767cd11961f445e91e2ff9e705e6a0d`, then the pipeline was exported in JSON
   and Python forms.
8. The published `1.15.0` host surface did not expose `build_review_packet`, although that tool was already present on
   the repository's unreleased `main` branch.

## Artifacts

| Artifact | SHA-256 | Observation |
| --- | --- | --- |
| [Baseline contact sheet](../assets/host-evidence/codex-2026-07-11-baseline.png) | `ecfa061227d00fc4f298e7623744e32d23d5258a11ee25375e640f655262270c` | Original fixture remained readable. |
| [Accepted contact sheet](../assets/host-evidence/codex-2026-07-11-accepted.png) | `e8e3b1950f362f57fd8200c32cc56b9c8c17a9f499ba05e0fa42fb254b484556` | Exposure changed while geometry and label remained readable. |

The contact sheets are both 160 x 120 RGB images. A pixel comparison found all 19,200 pixels changed, with mean
absolute RGB deltas of `11.3673`, `11.7036`, and `12.0418`; the difference is therefore not metadata-only.

## Product Findings

| Finding | Evidence-backed action |
| --- | --- |
| Direct image paths require an unnecessary parent-directory retry. | Accept one supported image as a bounded onboarding source. |
| Weak exposure has no structured feedback vocabulary. | Add a bounded `exposure_too_weak` adjustment with safety-conflict precedence. |
| Plugin guidance references unreleased tools. | Publish the current compatible tool surface and pin the plugin to `1.16.0`. |

## Privacy And Claim Boundary

No raw Codex transcript, user dataset, credentials, absolute local paths, or private host configuration is included.
The receipt supports the Codex manual-host and generated-fixture replay gates only. Claude Code remains blocked, and
all beta/adoption gates remain governed by their separate canonical records.
