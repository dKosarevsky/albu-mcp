[project]
name = "albumentationsx-mcp-desktop-extension"
version = "1.16.0"
description = "UV runtime wrapper for the AlbumentationsX MCP Claude Desktop extension."
requires-python = ">=3.10"
dependencies = ["albumentationsx-mcp==1.16.0"]

[tool.uv]
package = false
